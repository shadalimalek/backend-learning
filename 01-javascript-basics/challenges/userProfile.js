const user = {
    username: "Shad",
    age: 19,
    learning: "Backend Development",
    isMotivated: true
};

console.log(`Hi, I'm ${user.username} (${user.age}). Currently learning ${user.learning}. Motivated? ${user.isMotivated}`);